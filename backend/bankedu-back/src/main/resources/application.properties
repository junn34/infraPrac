# ----------------------------
# ? ?? ?? ??
# ----------------------------
server.port=8080
spring.application.name=bankedu-back

# ----------------------------
# ? MySQL ?????? ??
# ----------------------------

spring.datasource.url=jdbc:mysql://sw_team_6_mysql:3306/wooriLearn?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
spring.datasource.username=root
spring.datasource.password=1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# ----------------------------
# ? JPA / Hibernate ??
# ----------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ----------------------------
# ? CORS ?? (Next.js localhost)
# ----------------------------
# WebConfig??? ?????, ?? ?? ?? ??
spring.web.cors.allowed-origins=http://localhost:3000,http://127.0.0.1:3000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allow-credentials=true

# ----------------------------
# ? Spring Security ??? ?? ??
# ----------------------------
server.servlet.session.cookie.same-site=None
server.servlet.session.cookie.secure=false
server.servlet.session.timeout=30m

# ----------------------------
# ? ?? ??
# ----------------------------
logging.level.org.springframework=INFO
logging.level.com.bankedu=DEBUG

# ===============================
# ? ?? JSON ??/?? ??? ??
# ===============================
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true
